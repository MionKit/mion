---
type: feature
spec: guidelines
status: done
created: 2026-08-28
---

# Type-defined drizzle tables that mirror the builders

## Intent

Give mion users a pure-type way to define a TABLE, not just a row, mirroring the
column builders as closely as the type system allows:

```ts
const usersRT = DB.pgTable('users', {
  id: DB.uuid('id').primaryKey().defaultRandom(),
  name: DB.varchar('name', {length: 100}).notNull(),
  age: DB.integer('age').notNull(),
  bio: DB.varchar('bio', {length: 500}),
});

type UsersTable = DB.PgTable<'users', {
  id: DB.Uuid<'id'> & DB.PrimaryKey & DB.DefaultRandom;
  name: DB.Varchar<'name', {length: 100}> & DB.NotNull;
  age: DB.Integer<'age'> & DB.NotNull;
  bio: DB.Varchar<'bio', {length: 500}>;
}>;
```

Both roads must be equivalent: same inferred models, same runtype id, and the same
drizzle table out of materialization. This is exactly how TypeFormats already work:
the value builder and the type converge on one structural id
(packages/ts-runtypes/src/formats/scalars.ts records that principle). The team has
not yet decided whether mion recommends builders or types by default; this feature
makes the choice real by making the two roads interchangeable.

## Direction

The implementer designs the details; verified starting points:

- Today's named data types (Varchar, Uuid, ... in each dialect's src/columns.ts)
  carry only the DATA type. The type road additionally needs, per column: the db
  column NAME and the modifier flags. The builders' flags already live in
  RtColumnBrand<Data, NotNull, HasDefault, InsertExcluded>
  (packages/drizzle-orm/src/recorder.ts); the modifier types (DB.NotNull,
  DB.PrimaryKey, DB.DefaultRandom, ...) and the PgTable<Name, Cols> wrapper should
  compile down to (or be convertible into) that same brand, so
  InferSelectModel/InferInsertModel/InferUpdateModel and refineTableType
  (packages/drizzle-orm/src/models.ts, refine.ts) work unchanged on both roads.
- The modifier vocabulary mirrors the builder methods one to one (notNull,
  primaryKey, the default-ish flags, generatedAlwaysAs, ...). Runtime-function
  modifiers ($defaultFn, $onUpdate and friends) are explicitly OUT: they cannot be
  represented in a type, and mion does not rely on them anyway.
- The "same drizzle table" half needs a runtime bridge from a pure type: something
  like tableFromType<UsersTable>() that reaches the reflected type structure the
  same way the generated families do (the InjectTypeFnArgs cache path); the drizzle
  init state (builder fn, column name, params, flags) must be recoverable from the
  reflected format params, which already carry the per-column params today.
- The convert program must cover drizzle tables too. ts-runtypes convert
  (ts-go-runtypes/internal/convert) already rewrites declarations between the
  type-first and builder forms over the shared reflection graph. Once tables have
  two equivalent roads, conversion must translate a builder-authored drizzle table
  into its type-defined twin and back, without changing the structural id (the
  same id oracle the existing convert fuzz lane pins).
- No hardcoded drizzle names in the translator, the vocabulary must come from
  sources the program can read. The manifests
  (packages/drizzle-orm*/manifests/*.manifest.json, generated by
  ts-go-runtypes/cmd/gen-drizzle-manifest, the same files behind
  `pnpm rtx core drizzle-manifest --check`) are the obvious one, but not the
  only one: strict src patterns (like the uppercase naming rule below) and
  parsing the real package sources through the tsgo checker already embedded in
  the Go program are equally valid. Any mix of these is fine; a name table in
  Go code is not.
- The name mapping is a rule, not a list: a builder function maps to the type
  with the same name and the first letter uppercased (varchar to Varchar,
  pgTable to PgTable), in both directions.
- Verified gap: today's manifests list exported functions only (fn, kind, params,
  status); the chained modifier methods (notNull, primaryKey, defaultRandom, ...)
  are not recorded. If the translator needs the modifier vocabulary, extend
  gen-drizzle-manifest to record it; hardcoding it in the translator is not an
  option.
- Examples and docs: EVERY drizzle example gets an exact types twin (no single
  compare file); the docs render the builder and type versions side by side as two
  code blocks in a row. The mion overview's dialect trio and the refine/model
  examples are the anchor set; the new examples ride the tsconfig.drizzle.json
  typecheck gate in packages/examples.

## Oracle

- Same runtype id: getRunTypeId of the builder-inferred model equals the
  type-defined table's model, in BOTH marker call shapes (the Marker test coverage
  rule in ts-go-runtypes/CLAUDE.md).
- Same drizzle table: getTableConfig(toDrizzle(builderTable)) deep-equals
  getTableConfig(tableFromType<TypeTable>()); extend the pg fuzz suite
  (packages/drizzle-orm-pg-core/src/tableEquality.fuzz.spec.ts) to interpret each
  random spec over THREE surfaces (raw drizzle, builders, types).
- Convert round-trip: builders to type to builders (and the reverse) over the
  drizzle table corpus preserves the structural id and re-prints to an equivalent
  table; ride the existing convert fuzz lane.

## Done when

A table written either way yields the same models, the same runtype id, and the
same materialized drizzle table, pinned by the oracle tests above; ts-runtypes
convert translates drizzle tables both ways with no name list hardcoded in Go
(names come from the manifests, the naming rule, or the checker over the real
sources), pinned by the round-trip oracle; every drizzle example exists in both flavors
and the docs show them side by side.

## Shipped (2026-08-28)

Decisions taken with the developer, all implemented:

- Today's data-alias syntax (Varchar<100>, Timestamp<'date'>) is DISCARDED (breaking).
  The named types are column types only, params mirroring the builder arguments
  one to one: Varchar<'bio', {length: 500}> matches varchar('bio', {length: 500});
  name optional, config optional, config-only form supported. Old data-alias users
  switch to the core format types (Str<{maxLength}>, ...).
- FULL scope: references, table extras (index, uniqueIndex, unique, check,
  foreignKey, composite primaryKey) and literal sql values have type spellings.
- No runtime import of @ts-runtypes/core outside core: tableFromType(runType, deps?)
  takes a resolved RunType<T> (callers write tableFromType(getRunType<UsersRT>())).
  Dialect core peers stay type-only.
- HORIZONTAL implementation: the full pipeline (types, bridge, fuzz, convert) landed
  first on a thin pg slice, then widened batch by batch with every oracle green.

What landed:

- Core (@mionjs/drizzle-orm/src/typeColumns.ts): optional unique-symbol sentinels
  rtColSpecKey / rtColModsKey / rtSqlTextKey / rtEntrySpecKey (the FormatBrand
  pattern). RtColType<Fn, Name, Config, Data, BaseNotNull, BaseHasDefault> carries
  the builder call in the type (base flags cover serial-likes); marker interfaces
  (NotNull, PrimaryKey<Config?>, Default<V>, Unique, GeneratedAlwaysAs...,
  Autoincrement, OnUpdateNow, ColArray, $Type, References<Table, Column, Actions?>,
  Sql<Text>) merge under rtColModsKey (flag mods store true, arg mods store the
  args TUPLE). TableEntry<Fn, Args, Chain> is the mechanical extras carrier; the
  dialects export pretty per-helper aliases (IndexEntry, CheckEntry, ...).
  TypedCols normalizes intersections into RtColumnBrand + sentinels, feeding the
  existing RtTable so models/refine/ToDrizzleTable work unchanged (model ids equal
  by construction; the TABLE type's own id is deliberately NOT an oracle).
- Bridge (fromType.ts, core, no runtime core import): buildRtTableFromGraph walks
  the reflected graph and replays recorders; per-dialect tableFromType supplies
  the drizzle context, memoizes per runtype id and returns the slim table typed T
  (deps.tables resolves References/foreign refs by db table name).
- Dialects: every column type replaces its data alias, sharing one data helper
  with the builder overloads (cheap DataOf params form for builders, config-extract
  Data form for the type, keeping the type-instantiation budgets green). Wrappers
  PgTable/MysqlTable/SqliteTable<Name, Cols, Extras?>. Two exemptions: mysqlEnum
  (values-array arg, no config-object type spelling) and sqlite's int (alias of
  integer; Integer covers the type road).
- Go convert (internal/convert/drizzle.go, no name lists): declarations recognized
  by the resolved type's sentinel members; vocabulary = the upperFirst rule checked
  against the imported module's exports via a syntactic export walker (named
  re-exports from bare specifiers count); canonical pair value + type name preserved
  both directions (const usersRT + type UsersRT); extras print as the canonical
  TableEntry spelling type-side and the (t) => [...] callback builder-side; drizzle
  decls are exempt from the generic id/canonical oracles (model ids pinned JS-side).
  CNV009 refuses what has no twin: runtime-function modifiers ($-methods, $type
  included; the $Type marker still works in the type road and bridge), interpolated
  sql, references to consts outside the file, chain decorators on column refs in
  extras (t.name.desc()).
- gen-drizzle-manifest records per-column modifiers (chainable builder methods
  only, so sqlite's public build(table) materializer drops out structurally) and
  the typeAlias derived per the upperFirst rule against the proxy's type exports;
  per-dialect manifest-coverage specs gate both (every migrated column has its
  alias, every non-runtime modifier its marker export).
- Oracles: typeTables specs per dialect (getTableConfig projection equality + model
  id pairs in BOTH marker call shapes); the pg table-equality fuzz interprets each
  spec over THREE surfaces (raw drizzle / builders / synthetic-graph bridge);
  drizzleTypeSource.integration.spec.ts runs rendered type sources through the real
  resolver (rtx fuzz lane drizzletypes); Go TestFuzz_DrizzleRoundTrip rides the rtx
  convert fuzz lane with byte fixpoint; drizzleConvert.integration.spec.ts round
  trips the real CLI over a temp consumer project.
- Examples and docs: all 7 drizzle examples have exact types twins
  (drizzle-types-*.ts, on the tsconfig.drizzle.json gate); the mion overview
  renders builder and type versions side by side per example, with a new
  "The same table as a pure type" section and the convert one-liner.

Out of scope, unchanged: runtime-function modifiers ($defaultFn, $onUpdate,
function-valued anything) and sql with interpolations have no type spelling.
The sibling drizzle-code-translator todo is NOT implemented; the convert arm's
export-walker vocabulary is reusable for it.
