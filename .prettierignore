# package.json is formatted by package managers, so we ignore it here
package.json


########################
# generated files
########################
.dist
dist
.tmp
tmp
.coverage
coverage
.build
build

# Written by the type-budget suite on every run (packages/type-budget/test/report.ts).
# Prettier would realign the tables, the next run would write them back, and the
# file would churn forever.
packages/type-budget/reports

# Same, for the builder/format call-site budgets
# (packages/run-types/test/types/builderCostReport.ts).
packages/run-types/reports
