{
  "$comment": "HAND-OWNED. Tests the drizzle-e2e lane does not run, one array per dialect. Every entry carries a reason, and the reasons come in two kinds: what drizzle itself skips for the same driver, and where OUR boundary genuinely differs. A translated test that fails and is NOT listed here is a real recorder bug — add the FIX, not a row. run-suite.mjs REFUSES an entry that names no test in the vendored suite, so a stale row cannot sit here pretending to skip something.",
  "pg": [],
  "mysql": [
    {
      "test": "migrator",
      "reason": "the lane vendors a SUBSET of drizzle's suite tree (the three driver-agnostic <dialect>-common.ts files and their two helpers), and this test reads migration files from a drizzle2/ folder that is not part of it. Nothing to do with the recorders; drizzle's own pg runner skips its migrator tests for its own reasons too."
    },
    {
      "test": "MySqlTable :: select with `use index` hint",
      "reason": "a mysql index HINT is a query-side value: `.useIndex(idx)` wants drizzle's own index builder, while the same index is declared in the table's extraConfig as a recorder. One binding cannot be both, so the hint stays on drizzle's side of the boundary."
    },
    {
      "test": "MySqlTable :: select with `use index` hint on 1 index",
      "reason": "a mysql index HINT is a query-side value: `.useIndex(idx)` wants drizzle's own index builder, while the same index is declared in the table's extraConfig as a recorder. One binding cannot be both, so the hint stays on drizzle's side of the boundary."
    },
    {
      "test": "MySqlTable :: select with `use index` hint on multiple indexes",
      "reason": "a mysql index HINT is a query-side value: `.useIndex(idx)` wants drizzle's own index builder, while the same index is declared in the table's extraConfig as a recorder. One binding cannot be both, so the hint stays on drizzle's side of the boundary."
    },
    {
      "test": "MySqlTable :: select with `use index` hint on not existed index",
      "reason": "a mysql index HINT is a query-side value: `.useIndex(idx)` wants drizzle's own index builder, while the same index is declared in the table's extraConfig as a recorder. One binding cannot be both, so the hint stays on drizzle's side of the boundary."
    },
    {
      "test": "MySqlTable :: select with `use index` + `force index` incompatible hints",
      "reason": "a mysql index HINT is a query-side value: `.useIndex(idx)` wants drizzle's own index builder, while the same index is declared in the table's extraConfig as a recorder. One binding cannot be both, so the hint stays on drizzle's side of the boundary."
    },
    {
      "test": "MySqlTable :: select with join `use index` hint",
      "reason": "a mysql index HINT is a query-side value: `.useIndex(idx)` wants drizzle's own index builder, while the same index is declared in the table's extraConfig as a recorder. One binding cannot be both, so the hint stays on drizzle's side of the boundary."
    },
    {
      "test": "MySqlTable :: select with join `use index` hint on 1 index",
      "reason": "a mysql index HINT is a query-side value: `.useIndex(idx)` wants drizzle's own index builder, while the same index is declared in the table's extraConfig as a recorder. One binding cannot be both, so the hint stays on drizzle's side of the boundary."
    },
    {
      "test": "MySqlTable :: select with cross join `use index` hint",
      "reason": "a mysql index HINT is a query-side value: `.useIndex(idx)` wants drizzle's own index builder, while the same index is declared in the table's extraConfig as a recorder. One binding cannot be both, so the hint stays on drizzle's side of the boundary."
    },
    {
      "test": "MySqlTable :: select with cross join `use index` hint on 1 index",
      "reason": "a mysql index HINT is a query-side value: `.useIndex(idx)` wants drizzle's own index builder, while the same index is declared in the table's extraConfig as a recorder. One binding cannot be both, so the hint stays on drizzle's side of the boundary."
    },
    {
      "test": "MySqlTable :: select with join `use index` hint on multiple indexes",
      "reason": "a mysql index HINT is a query-side value: `.useIndex(idx)` wants drizzle's own index builder, while the same index is declared in the table's extraConfig as a recorder. One binding cannot be both, so the hint stays on drizzle's side of the boundary."
    },
    {
      "test": "MySqlTable :: select with join `use index` hint on not existed index",
      "reason": "a mysql index HINT is a query-side value: `.useIndex(idx)` wants drizzle's own index builder, while the same index is declared in the table's extraConfig as a recorder. One binding cannot be both, so the hint stays on drizzle's side of the boundary."
    },
    {
      "test": "MySqlTable :: select with join `use index` + `force index` incompatible hints",
      "reason": "a mysql index HINT is a query-side value: `.useIndex(idx)` wants drizzle's own index builder, while the same index is declared in the table's extraConfig as a recorder. One binding cannot be both, so the hint stays on drizzle's side of the boundary."
    },
    {
      "test": "MySqlTable :: select with Subquery join `use index`",
      "reason": "a mysql index HINT is a query-side value: `.useIndex(idx)` wants drizzle's own index builder, while the same index is declared in the table's extraConfig as a recorder. One binding cannot be both, so the hint stays on drizzle's side of the boundary."
    },
    {
      "test": "MySqlTable :: select with Subquery join with `use index` in join",
      "reason": "a mysql index HINT is a query-side value: `.useIndex(idx)` wants drizzle's own index builder, while the same index is declared in the table's extraConfig as a recorder. One binding cannot be both, so the hint stays on drizzle's side of the boundary."
    },
    {
      "test": "View :: select with `use index` hint",
      "reason": "a mysql index HINT is a query-side value: `.useIndex(idx)` wants drizzle's own index builder, while the same index is declared in the table's extraConfig as a recorder. One binding cannot be both, so the hint stays on drizzle's side of the boundary."
    },
    {
      "test": "Subquery :: select with `use index` hint",
      "reason": "a mysql index HINT is a query-side value: `.useIndex(idx)` wants drizzle's own index builder, while the same index is declared in the table's extraConfig as a recorder. One binding cannot be both, so the hint stays on drizzle's side of the boundary."
    }
  ],
  "sqlite": [
    {
      "test": "transaction",
      "reason": "better-sqlite3 is a SYNCHRONOUS driver and rejects an async transaction callback (\"Transaction function cannot return a promise\"). Nothing to do with the recorders: drizzle skips two of these itself for the same driver, and the other two fail in better-sqlite3s own transaction wrapper."
    },
    {
      "test": "transaction rollback",
      "reason": "better-sqlite3 is a SYNCHRONOUS driver and rejects an async transaction callback (\"Transaction function cannot return a promise\"). Nothing to do with the recorders: drizzle skips two of these itself for the same driver, and the other two fail in better-sqlite3s own transaction wrapper."
    },
    {
      "test": "nested transaction",
      "reason": "better-sqlite3 is a SYNCHRONOUS driver and rejects an async transaction callback (\"Transaction function cannot return a promise\"). Nothing to do with the recorders: drizzle skips two of these itself for the same driver, and the other two fail in better-sqlite3s own transaction wrapper."
    },
    {
      "test": "nested transaction rollback",
      "reason": "better-sqlite3 is a SYNCHRONOUS driver and rejects an async transaction callback (\"Transaction function cannot return a promise\"). Nothing to do with the recorders: drizzle skips two of these itself for the same driver, and the other two fail in better-sqlite3s own transaction wrapper."
    }
  ]
}
