// smoke-webpack — LIGHT smoke: the webpack adapter builds the shared MINIMAL
// subset (TS via esbuild-loader, ESM output, @ts-runtypes/core external).
export {selfCheck} from '../../shared/src/minimal';
