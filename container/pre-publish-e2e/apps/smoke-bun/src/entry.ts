// smoke-bun — LIGHT smoke over Bun's BUNDLER host (`Bun.build`), driven through
// @mionjs/devtools/runtypes/bun. Same shared minimal subset and same dist/entry.js
// contract as the other five adapters, so test/build-outputs.test.mjs treats it
// identically.
export {selfCheck} from '../../shared/src/minimal';
