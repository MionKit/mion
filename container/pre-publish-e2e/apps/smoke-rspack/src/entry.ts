// smoke-rspack — LIGHT smoke: the rspack adapter builds the shared MINIMAL subset
// (TS via rspack's builtin SWC loader, ESM output, @mionjs/run-types external).
export {selfCheck} from '../../shared/src/minimal';
