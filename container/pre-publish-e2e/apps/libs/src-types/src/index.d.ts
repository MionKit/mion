// What a plain consumer resolves through `types`; the package has no dist.
export interface SrcTypedUser {
  id: number;
  label: string;
  since: Date;
}
export declare function registerThing(name: string): void;
