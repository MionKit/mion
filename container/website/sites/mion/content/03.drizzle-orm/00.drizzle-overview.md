---
title: Drizzle Overview
description: Declare Drizzle ORM tables as usual, get lightweight models with full validation fidelity, and materialize the real table on demand.
toc: false
---

One package per database gives you the [Drizzle ORM](https://orm.drizzle.team) table authoring surface for that dialect: the same column builders, the same names and parameters, the same modifier chains, the same indexes and constraints. You write your tables exactly as you would with drizzle.

| Database | Package |
|----------|---------|
| PostgreSQL | `@mionjs/drizzle-orm-pg-core` |
| MySQL | `@mionjs/drizzle-orm-mysql-core` |
| SQLite | `@mionjs/drizzle-orm-sqlite-core` |
| Cloudflare D1, Durable Objects | `@mionjs/drizzle-orm-sqlite-core` |

Cloudflare's two databases are both SQLite, so they share the SQLite package. You only pick a different driver where you connect: see [Cloudflare D1 and Durable Objects](/drizzle-orm/cloudflare-storage).

Each package version follows drizzle-orm, not mion: version 0.45.x works with drizzle-orm 0.45.x, and the package declares that range as a peer dependency. Pick the package line that matches the drizzle-orm version in your project. A new version is published only when the package itself changes, so the same version can stay current across several mion releases.

Install the dialect package with its shared core. Drizzle itself is an optional peer: your schema, models and validators work without it, and you add it when you talk to a database:

```bash
npm install @mionjs/drizzle-orm-pg-core @mionjs/drizzle-orm @mionjs/run-types
npm install drizzle-orm   # only needed to run queries or migrations
```

The difference is under the hood. Drizzle's own table types are heavy, and they forget most column parameters (a varchar length, whether an int is unsigned). Our builders record your calls instead of running drizzle, and their types carry exactly what your API needs: the matching format type from `@mionjs/run-types/formats` with the parameters you wrote, plus the not-null and default flags. Inferred models are small flat types, editors stay fast, and the compiled validators enforce every captured parameter with no extra runtime checks.

::code-group
<code-import path="packages/examples/src/drizzle/drizzle-proxy-pg-example.ts" lang="ts [PostgreSQL]" />

<code-import path="packages/examples/src/drizzle/drizzle-proxy-mysql-example.ts" lang="ts [MySQL]" />

<code-import path="packages/examples/src/drizzle/drizzle-proxy-sqlite-example.ts" lang="ts [SQLite]" />
::

Because routes and tables share the same model types, a route typed with the inferred model uses the exact same compiled validator function as any other consumer of that model.

## The same table as a pure type

Every table can also be written as a type instead of builder calls. Each column builder has a column type with the same name and the first letter uppercased. It takes the database column name, then one object holding everything else: the builder's own config keys, plus the modifiers you would have chained. So `Varchar<'name', {length: 100; notNull: true}>` matches `varchar('name', {length: 100}).notNull()`.

A modifier called with no arguments is `true`, one called with arguments is the list of arguments: `{primaryKey: true}` for `.primaryKey()`, `{default: [21]}` for `.default(21)`. The two forms are interchangeable: same inferred models, same validators, and the same recorded table behind them, without repeating the type name anywhere. Here is the trio above, written as types:

::code-group
<code-import path="packages/examples/src/drizzle/drizzle-types-pg-example.ts" lang="ts [PostgreSQL]" />

<code-import path="packages/examples/src/drizzle/drizzle-types-mysql-example.ts" lang="ts [MySQL]" />

<code-import path="packages/examples/src/drizzle/drizzle-types-sqlite-example.ts" lang="ts [SQLite]" />
::

Pick whichever form reads better for you, or mix them per table. The `mion convert` command translates a table declaration between the two forms in either direction, so switching is one command, not a rewrite.

## The real drizzle table, on demand

What pgTable returns is a recorded table, not drizzle's own table type. It captures everything drizzle needs, and `toDrizzle` builds the real drizzle table from that record the first time you ask for it (every later call returns the same object). That is the one place drizzle runs, so its heavy types are paid only in the files that run queries, never in your routes or your client:

<code-import path="packages/examples/src/drizzle/drizzle-to-drizzle-example.ts" lang="ts" />

A table written as a pure type gets there in one step: toDrizzle takes the table type directly and builds the same memoized drizzle table, so a query file needs nothing from the schema file except the type itself.

<code-import path="packages/examples/src/drizzle/drizzle-types-to-drizzle-example.ts" lang="ts" />

The same call feeds migrations: a drizzle-kit schema file exports `toDrizzle(users)` on the builders road, or `toDrizzle<UsersTable>()` on the types road (and the materialized enums, for `pgEnum`). Everything on the query side of drizzle works on the `toDrizzle` result as usual: operators, relations, getTableConfig, and views built from a query builder.

For files that need the recorded table without drizzle (models, refinement, mion routes), `tableFromType<UsersTable>()` from the package root is the same bridge without the drizzle import. Its options object also carries the two runtime inputs a type cannot: the tables a column References (`{tables: {parents: parents}}`, or `{tables: {parents: () => parents}}` when the referenced table is declared further down the file) and the runtime callbacks described below.

## Refine tables for your API

Your API often needs stricter rules than the database column: a name column can hold 100 characters, but the signup form wants at least 10. `refineTableType` adds those rules at the type level. It returns the same table object (nothing about the SQL changes) with the extra format parameters merged into the captured ones. Derive your model types from the refined table and the whole toolbox compiles from them: validators that enforce both the database rules and the refined ones, mock data that respects every bound, and JSON serializers that keep Date columns real Dates across the wire. This works with or without mion:

<code-import path="packages/examples/src/drizzle/drizzle-refine-example.ts" lang="ts" />

The same refinement over a table written as a pure type:

<code-import path="packages/examples/src/drizzle/drizzle-types-refine-example.ts" lang="ts" />

On the types road the same refinement is a type, no table value needed: `RefinedTable` takes the table type and the per-column rules, so a types-only file can export the API view of a table.

```ts
import type {RefinedTable} from '@mionjs/drizzle-orm';
import type {UsersTable} from './schema.ts';

type ApiUsersTable = RefinedTable<UsersTable, {name: {minLength: 10}; age: {min: 18}}>;
```

A refinement that does not fit the column is a compile error, never a silent override: you cannot put a number rule on a string column, invent an unknown parameter, or change whether a column accepts null (that stays with notNull in the table itself). Columns without a format (booleans, JSON, enums) are not refinable.

`InferSelectModel`, `InferInsertModel` and `InferUpdateModel` (the exact names drizzle uses) come from `@mionjs/drizzle-orm`, together with refineTableType and the sql template. They derive the row, insert and update payload types from a table (refined or not): inserts drop generated columns and make defaulted ones optional, updates accept any subset of the insert payload.

## Use the models in mion routes

Type your routes with the derived models and the whole default behavior is already generated from the types: payloads are validated before the handler runs (the refined bounds included), Dates are serialized and revived on both ends of the wire, the update route accepts any subset while still validating present keys, and the client gets full types from the exported api type. There is nothing to register per route:

<code-import path="packages/examples/src/drizzle/drizzle-mion-routes-example.ts" lang="ts" />

Because both table forms infer identical models, the same routes work unchanged over a table written as a pure type; only the import line differs:

<code-import path="packages/examples/src/drizzle/drizzle-types-mion-routes-example.ts" lang="ts" />

## Insert, select and update payloads

The three model types share their names with drizzle, so a schema file ports by changing only the import:

<code-import path="packages/examples/src/drizzle/drizzle-model-types.ts" lang="ts" />

The same payloads derive straight from a table written as a pure type, with no table value in sight; a file like this is types only from top to bottom:

<code-import path="packages/examples/src/drizzle/drizzle-types-model-types.ts" lang="ts" />

## Runtime column callbacks

Drizzle's `$default`, `$defaultFn`, `$onUpdate` and `$onUpdateFn` take a function the application runs on insert or update. On the builders road they chain as usual. On the types road a function cannot live inside a type, so the column carries a marker with the same name (`$DefaultFn` for `.$defaultFn(...)`, and so on) and the callback itself goes into the options object of tableFromType or toDrizzle. The markers keep the model types right everywhere: a column with any of them counts as having a default, so it turns optional on insert even in files that only import the type.

<code-import path="packages/examples/src/drizzle/drizzle-types-runtime-example.ts" lang="ts" />

The `mion convert` command moves the callbacks between the two spellings for you, text intact, in both directions.

## Semantic types by hand

Columns whose values have no matching format (geometry, vectors, MAC addresses, binary data, plain booleans and JSON) keep their plain data type. For JSON columns, `.$type<T>()` stays the escape hatch to declare a semantic type by hand, exactly as in drizzle:

```ts
import * as DZ from '@mionjs/drizzle-orm-pg-core';

const events = DZ.pgTable('events', {
  id: DZ.uuid('id').primaryKey(),
  payload: DZ.jsonb('payload').$type<{kind: string; at: number}>().notNull(),
});
```

The same override on the types road is the `$type` prop:

```ts
import * as DZ from '@mionjs/drizzle-orm-pg-core';

type EventsTable = DZ.PgTable<
  'events',
  {
    id: DZ.Uuid<'id', {primaryKey: true}>;
    payload: DZ.Jsonb<'payload', {$type: [{kind: string; at: number}]; notNull: true}>;
  }
>;
```

::note
The full list of what each column builder carries, per database and column mode, lives in [Column Formats](./column-formats).
::
