---
title: Indexes and Constraints
description: Indexes, unique constraints, checks, foreign keys and composite keys, written exactly as in Drizzle.
---

Constraints and indexes go in the table's third argument: a callback that receives the columns and returns an array. Same names, same parameters, same chains as Drizzle.

None of this reaches your app types. Constraints shape the SQL, not the rows you read, so they cost your models nothing.

<code-import path="packages/examples/src/drizzle/drizzle-constraints-example.ts" lang="ts" />

## What you can put in the array

| Helper | What it makes |
| --- | --- |
| `index(name)` | a plain index |
| `uniqueIndex(name)` | a unique index |
| `unique(name)` | a unique constraint |
| `primaryKey({name, columns})` | a composite primary key |
| `foreignKey({name, columns, foreignColumns})` | a foreign key |
| `check(name, sql)` | a check constraint |
| `pgPolicy(name, config)` | a row level security policy, PostgreSQL only |

## Chaining

Indexes take the options PostgreSQL takes:

```ts
index('idx').on(t.name.desc(), t.age).where(sql`${t.age} >= 18`);
uniqueIndex('idx').using('hnsw', t.embedding.op('vector_l2_ops'));
index('idx').on(t.name).concurrently().with({fillfactor: '70'});
```

Column position modifiers are `asc()`, `desc()`, `nullsFirst()`, `nullsLast()` and `op()`. Foreign keys take `onDelete()` and `onUpdate()`. Unique constraints take `nullsNotDistinct()`. MySQL indexes also take `algorithm()` and `lock()`.

## Inline on the column

Single column constraints can sit on the column instead, which reads better when there is only one:

```ts
id: uuid('id').defaultRandom().primaryKey(),
code: varchar('code', {length: 12}).notNull().unique('invites_code_uq'),
teamId: integer('team_id').references(() => cols(teams).id, {onDelete: 'cascade'}),
```

## Pointing at another table

A foreign key names a column on another table, and it reaches it through `cols()`:

```ts
import {cols} from '@mionjs/drizzle-orm';

references(() => cols(teams).id);
foreignKey({name: 'members_team_fk', columns: [t.teamId], foreignColumns: [cols(teams).id]});
```

A table's type is its shape (the name, the columns, the constraints), not the columns
themselves, which keeps that type small everywhere it travels through your app. `cols()`
opens it up when you need one column out of it. Columns of the table you are declaring
need nothing: they arrive as the callback argument, `t.teamId` above.
