<!-- Local override of docus/app/components/docs/DocsAsideLeftTop.vue. With
     `navigation.sub: 'aside'` Docus would list the subsites at the top of every
     sidebar; the header tabs already do that job, so the slot stays empty and the
     sidebar starts straight at the current subsite's sections. -->
<template>
  <div />
</template>
