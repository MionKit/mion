<!-- /benchmarks is the subsite root and redirects to its home page, /benchmarks/introduction/mion-benchmarks (the first
     docs page of its sidebar; app/utils/subsites.ts). public/_redirects carries the same
     redirect for the static host. -->
<script setup lang="ts">
definePageMeta({redirect: '/benchmarks/introduction/mion-benchmarks'})
</script>

<template>
  <div />
</template>
