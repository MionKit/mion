---
title: Source Conversion
description: Rewrite files between plain types, type builders and JSON Schema with one command.
toc: false
---

## One shape, any notation

RunTypes accepts three ways to write a shape: a plain TypeScript type, the value-first type builders, and a JSON Schema. They are interchangeable, so a codebase can also switch between them. The convert command rewrites your files from one form to another, in place.

```bash
# one file, or several
npx ts-runtypes convert --to builders src/models.ts src/api.ts

# or a whole folder (every .ts and .tsx inside it)
npx ts-runtypes convert --to json-schema src/models/
```

The targets are `type`, `builders` and `json-schema`. Declarations already written in the target form are left byte-for-byte unchanged, so running the command twice is safe.

<code-import path="packages/examples/src/guide/convert-forms.ts" lang="ts" commentStart="// start-before" commentEnd="// end-before" />

After `--to builders` the declaration reads like this. The type name survives as an alias, so every import of it keeps working. (The example below renames the alias only so all three spellings can sit in one file; the real command keeps your original name.)

<code-import path="packages/examples/src/guide/convert-forms.ts" lang="ts" commentStart="// start-after-builders" commentEnd="// end-after-builders" />

And after `--to json-schema`:

<code-import path="packages/examples/src/guide/convert-forms.ts" lang="ts" commentStart="// start-after-schema" commentEnd="// end-after-schema" />

## Types written inside a call

Not every type has a declaration. Writing one straight into a factory call is
just as common, and there is nothing there for the converter to rename, so it
rewrites the call itself.

<code-import path="packages/examples/src/guide/convert-forms.ts" lang="ts" commentStart="// start-call-sites" commentEnd="// end-call-sites" />

Converting to type builders moves the type into the value slot the same factory
already accepts:

<code-import path="packages/examples/src/guide/convert-forms.ts" lang="ts" commentStart="// start-call-sites-after" commentEnd="// end-call-sites-after" />

Two shapes are deliberately left alone. A call that already names its type
(`createValidateFn<Order>()`) keeps the name, because the declaration behind it
converts on its own. And a call that reflects a runtime value
(`createValidateFn(order)`) is untouched, since there is no written type there
to move.

## Identity never moves

Conversion changes the spelling, never the shape. Every converted declaration resolves to the same identity as before, which means the same generated validator, the same codecs, the same mock data. This is enforced, not aspirational: the converter's test suite converts randomly generated files through the full chain (type to builders to JSON Schema and back) and checks the identity of every declaration on every step.

<code-import path="packages/examples/src/guide/convert-forms.ts" lang="ts" commentStart="// start-identity" commentEnd="// end-identity" />

## What converts

Everything the engine reflects: primitives and literals, string and number formats, arrays, tuples (labeled ones included, using RT.slot in builder form and the tsLabels keyword in schema form), objects (optional, readonly and quoted keys included), records (including an index signature beside named properties, which becomes an intersection in builder form and properties beside additionalProperties in schema form), unions, enums, classes, Date, Map, Set, Promise, RegExp, Temporal types, functions (named parameters use RT.slot too), template literals, branded types, and recursive types. Recursive shapes come out as `RT.circular` with `RT.self()` in builder form and as a `$ref: '#'` reference in schema form.

Declarations that reference each other stay references. A type that names another converted type keeps the name in every form, across files too: convert the files together and the imports are updated for you.

```bash
npx ts-runtypes convert --to builders src/user.ts src/order.ts
```

## What does not convert

Most shapes that the target form has no word for still convert: they ride an escape that carries the type itself, `getRunType<T>()` in builder form and `embedType<T>()` in schema form. That covers functions, template literals, bigint literals and more.

What is left is the short list below, where even the escape cannot help because the type cannot be written down inside it. The converter never guesses: it reports the shape, leaves that declaration exactly as you wrote it, converts the rest of the file, and exits with a non-zero code so CI notices.

| Shape | Why |
| ----- | --- |
| A cycle that never passes through a named type | The self reference has nothing to point at. Give the inner type a name and it converts. |
| A symbol-keyed property | The property name is a symbol, and an escape can only carry a type it can write down. |
| A recursive type reached only inside an embedded type expression | Embedded types are quoted TypeScript, and quoted text cannot point back at the declaration being defined. |
| A type that loops back through one of its own tuple slots, like `type Pair = [number, Pair]` | Only when converting to type builders or JSON Schema. TypeScript works out a tuple's slots straight away rather than on demand, so neither form can close the loop there. Put the recursion behind an object, array, Map, Set or function and it converts. |
| An exclusive union with a plain value branch, inside a recursive type | Only when converting to builder form. The plain type and JSON Schema forms carry it. |
| An exclusive union that is not the whole union, like `OneOf<[A, B]> \| C` | The extra arm has no spelling that survives the trip in any form. Give the exclusive part its own named type and the rest converts. |
| A schema carrying unevaluatedProperties or unevaluatedItems | The sweep has no spelling in the other forms yet, and it changes what the validator rejects, so dropping it would quietly weaken your validation. The declaration stays in schema form and keeps working. |
| A reference to a type whose file is not in the same run | Add that file to the same command. |
| A Temporal type that resolves to `any` | The Temporal declarations are missing from your project. Add the `ESNext.Temporal` lib or a polyfill's types. Converting would bake the `any` in permanently. |
| Anything needing the RunTypes dialect, under `--portable` | Only with that flag, and deliberately: see below. |

A generic type (`type Box<T> = ...`) is not on the list, and not an error
either: a type parameter has no runtime shape, so there is nothing to convert.
The declaration is reported and left as written, and every place it is used with
real types converts normally.

That list is kept honest by a test. Every row is a real declaration handed to the real command, asserting the exact message you would see and that your file came back untouched: [unsupported-conversion.test.ts](https://github.com/MionKit/ts-run-types/blob/main/packages/ts-runtypes/test/features/unsupported-conversion.test.ts). When one of these starts converting, that test fails and the row goes away, so the list can never quietly drift from the tool.

One more case is not a refusal but worth knowing: converting to plain types leaves a builder const alone if your code still uses it elsewhere, since removing it would break those call sites.

## JSON Schema to types mapping

Plenty of TypeScript has no word in standard JSON Schema: a bigint, a Temporal date, a function signature, a branded string, a template literal type, a readonly property. RunTypes adds keywords for them, so those types still convert as ordinary data. Each keyword sits beside a plain description of the JSON that actually travels, so the schema stays valid for any JSON Schema tool. The full keyword set, with the type each one maps to, is on [JSON Schema JS](/guide/json-schema-js).

<code-import path="packages/examples/src/guide/convert-forms.ts" lang="ts" commentStart="// start-dialect" commentEnd="// end-dialect" />

The few types whose identity is a name rather than a shape (a class, an enum member, a reference to another declaration) ride the embedType escape, which places a real TypeScript type inside the schema without losing anything. A function goes either way: with all parameters required it converts as data, names included; with an optional or rest parameter it takes the escape.

If you need schemas with no extension at all, pass `--portable`. It forbids every RunTypes keyword and the embedType escape, and a declaration that cannot be expressed without them becomes an error instead. Setting `convertDialect: 'standard'` in your tsconfig plugin entry makes that the project default, described under [configuration](/introduction/configuration); the flag still wins for a single run, in either direction.

## Checking without writing

`--check` reports what would change and exits with code 1 while anything is pending, so it slots into CI. `--out-dir` writes the converted files into a copy of the folder and leaves your sources untouched.

```bash
npx ts-runtypes convert --to type --check src/models/
npx ts-runtypes convert --to builders src/models/ --out-dir converted/
```

Anything the converter cannot express is reported with a clear message and left exactly as it was, and the exit code tells you something needs attention. The shapes that can happen to are listed above.

<!-- code-import-timestamp 1786911808421 -->
