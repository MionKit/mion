---
title: Types vs Schemas
description: Describe a shape as a TypeScript type, with schema builders, or as a JSON Schema; all three compile to the same thing.
toc: false
---

## Side by side

Same shape, three spellings. `isProductA`, `isProductB` and `isProductC` are identical validators under the hood.

::div{class="rt-define-cols rt-define-cols-3"}
:::code-group
<code-import path="packages/examples/src/guide/types-vs-schemas-side-by-side.ts" lang="ts [Type Definition]" commentStart="// start-type" commentEnd="// end-type" />
:::

:::code-group
<code-import path="packages/examples/src/guide/types-vs-schemas-side-by-side.ts" lang="ts [Schema]" commentStart="// start-schema" commentEnd="// end-schema" />
:::

:::code-group
<code-import path="packages/examples/src/guide/types-vs-schemas-side-by-side.ts" lang="ts [JSON Schema]" commentStart="// start-json-schema" commentEnd="// end-json-schema" />
:::
::

The pure type is the fast path. There's nothing to write but the type you already have. The schema is a real value you can store in a variable, pass to a function, or build up piece by piece. The JSON Schema form is for when you already have one, from an OpenAPI document or a config file, and would rather not translate it by hand. See [JSON Schema](/guide/json-schema) for what it accepts.

## Get the type back with `InferType`

A schema is a value, but sometimes you want the TypeScript type it stands for. `InferType<typeof schema>` hands it back:

<code-import path="packages/examples/src/guide/types-vs-schemas-static.ts" lang="ts" />

So you never write a shape twice. Start from a type and reflect it, or start from a schema and `InferType` your way to the type. A JSON Schema has the same door, `FromJsonSchema<typeof schema>`. Every direction gives you one source of truth.

## Mix them freely

They're the same thing, so you can use both in the same file: a plain type here, a schema there, even nesting one kind of thinking inside the other.

<code-import path="packages/examples/src/guide/types-vs-schemas-mixed.ts" lang="ts" />

Either way you end up at the same validator, the same JSON codec, the same mock. Pick whichever reads better to you.

## Reuse shared fields

Schemas are plain values, so you can keep a set of shared fields in one place and spread it into each schema that needs them. The merge happens at build time, so every schema still reflects its full, exact type (the same one you would get by writing every field out).

```ts
import {object, number, string, boolean} from '@ts-runtypes/core';

const base = {id: number(), createdAt: number()};

const User = object({...base, name: string()});      // {id, createdAt, name}
const Post = object({...base, published: boolean()}); // {id, createdAt, published}
```

The same works for shared option presets. A `createValidateFn` or `createJsonEncoderFn` call can spread a preset and override individual keys inline (a key written after the spread wins).

```ts
import {createJsonEncoderFn} from '@ts-runtypes/core';

const preset = {strategy: 'mutate'} as const;

const encodePost = createJsonEncoderFn<Post>(undefined, {...preset});
```

The fragment you spread has to be a `const` (or written inline), and it can live in another module and be imported. A value produced at runtime, like the result of a function call, cannot be read at build time, so spreading one is reported as an error.

<!-- code-import-timestamp 1785525102955 -->
