---
title: Validation Formats
description: How fast each library validates typed formats like email, uuid, and ipv4.
toc: false
---

The JSON Schema section at the end covers the value constraints a schema document carries, like a format, a numeric range, a pattern, or a divisor. RunTypes reads the document itself, and every other library states the same constraint in its own dialect.

::bench-table{bench="validation-formats" metric="validate"}
::
