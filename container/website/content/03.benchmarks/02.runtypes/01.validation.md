---
title: Validation
description: How fast each library runs validate, the fast boolean check that a value matches its type.
toc: false
---

The last section on this page is strict validation. There a value is accepted only when it
matches its type and carries nothing else. An extra key is a rejection, not something
quietly ignored.

::bench-table{bench="validation" metric="validate"}
::
