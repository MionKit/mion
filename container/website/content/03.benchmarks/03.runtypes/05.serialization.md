---
title: Serialization
description: How fast each strategy encodes, transmits and decodes a value across real-world types.
toc: false
---

Each chart is the geometric mean over the cases in that group that every strategy supports. The link on each chart opens those cases on GitHub.

Speed is the encode pass, with decode after the dot. Bytes are the encoded payload, where fewer is better.

:runtypes-bench-bars{bench="serialization" metric="encdec,payload"}
