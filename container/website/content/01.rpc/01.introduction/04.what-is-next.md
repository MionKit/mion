---
title: What's Next
description: Upcoming features and improvements for mion.
toc: false
---

## Top Priorities

We plan to finish these before leaving beta, but it could still take a while 😂

::card
---
title: Mature API
---
mion is in early development, so the first priority is a robust, developer-friendly API.

After v1 we want long release cycles and few new features, to avoid maintainer burnout.
::

::card
---
title: RunType Forms and UI
---
Generate forms from types, with components that work in multiple frontend frameworks.
This will give many apps a default UI and speed up work on our own dashboard.

```tsx
<script setup lang="ts">
type User = {
  email: Email
  birthDate: StringDate
}

const userFormSchema = toUIVueForm<User>()

function handleSubmit(data: User) {
  routes.users.create(data).call()
}
</script>

<template>
  <FormRenderer
    :schema="userFormSchema"
    @submit="handleSubmit"
  />
</template>
```
::

::card
---
title: Single Pass With TypeScript 7
---
RunTypes already runs on the TypeScript 7 Go compiler, but as a second pass beside `tsgo`, because typescript-go has no hook for runtime libraries yet ([microsoft/typescript-go#516](https://github.com/microsoft/typescript-go/issues/516)).
::
