---
title: Client Overview
description: Use the mion client for type-safe API calls.
toc: false
---

Routes are called like any other async function. The client only imports the API type from the server, so no server code ever reaches the browser.

::code-split
<code-import path="packages/private-examples/src/client/hello-client.ts" lang="ts [client.ts]" />
<code-import path="packages/private-examples/src/client/hello.routes.ts" lang="ts [server.routes.ts]" />
::

## Features

| Feature | What you get |
| --- | --- |
| Typed remote methods | Every route and middleware shows up with its params and return type, with autocompletion and static type checking. |
| Never throws | The result and the error come back together, so there is nothing to wrap in try/catch. |
| Validation and serialization | Params and results are validated and serialized out of the box, including types like Date, Map and Set. |
| Local validation | Wrong params are caught in the browser, with no request to the server. |
| Local sanitizing | The same transforms the route declares (trim, lowercase, strip separators) run before validation and before sending. |
| Middleware hooks | A middleware gets its data, like an auth token, from a hook that runs before every request. Its response hooks can retry the call. |
| Route sync | A call built against outdated route types is stopped before it runs, see [Keeping Client and Server in Sync](/rpc/client/route-sync). |

## Using the Client

Initialize the client with the API type `mion.initRoutes` returns (or `PublicApi<typeof routes>`). Import only the type, never the routes themselves.

::code-split
<code-import path="packages/private-examples/src/client/client-init.ts" lang="ts [client.ts]" />
<code-import path="packages/private-examples/src/client/init.routes.ts" lang="ts [server.routes.ts]" />
::

::note
By default the build ships each route's validation and serialization functions with the client, see [Bundling the API Into the Client](/rpc/client/bundled-api). With `bundleApi: false`, the client fetches them from the server on a route's first call instead, once you set up `useMethodsMetadata`, see [What the Client Remembers](/rpc/client/metadata-cache).
::

## Calling Routes

The `routes` and `middlewares` objects hold every remote route and middleware, except a middleware that takes no params and returns no data. Calling a route creates a `RouteSubRequest`, and `call()` sends it.

::code-split
<code-import path="packages/private-examples/src/client/client-calling-routes.ts" lang="ts [client.ts]" />
<code-import path="packages/private-examples/src/client/sum.routes.ts" lang="ts [server.routes.ts]" />
::

:read-more{to="/rpc/client/error-handling" title="Error Handling"}

## Sending Middleware Data

A middleware gets its data, like an auth token, from its `onRequest` hook. Call the function the hook receives with the middleware params, or skip the call to send nothing.

::code-split
<code-import path="packages/private-examples/src/client/client-middleware-hooks.ts" lang="ts [client.ts]" />
<code-import path="packages/private-examples/src/client/middleware-hooks.routes.ts" lang="ts [server.routes.ts]" />
::

| Hook | Runs | Removed with |
| --- | --- | --- |
| `onRequest` | Before each request that runs the middleware, a route's first call included. It can be async. Its second argument is the request, to pick data per route. | `offRequest()` |
| `onResponse` | After each successful middleware result, strongly typed. | `offResponse()` |
| `onError` | After each declared error of the given `type`, strongly typed. | `offError(type)` |

::tip
If `onRequest` throws or rejects, the request is not sent and `undeclared` holds the error. Undeclared errors (transport, platform, framework) never reach `onError`.
::

### Retrying a Call

`onResponse` and `onError` get the call as a second argument. Its `retry()` sends the whole call again, for example after refreshing a token, and runs every `onRequest` first. Each middleware can retry once per call.

| Route | Retry allowed |
| --- | --- |
| `query()` | Always |
| `mutation()` or `route()` | Only if the call failed: the route returned an error, or it returned nothing and the response has an error |

::note
`retry()` returns `false` when it is not allowed, and the call keeps its current result.
::

### Reusable Middleware

A middleware can ship with a client installer, a function that receives `middlewares.x` and sets its hooks. Type its parameter with `ClientMiddlewareOf<typeof handler>`, so it works wherever the app places the middleware.

<code-import path="packages/private-examples/src/client/client-middleware-installer.ts" lang="ts" />

::tip
A `bundleApi` build fails when a called route runs a middleware and the client never uses `middlewares.x`: `MET008` when it needs params, `MET009` when they are all optional.
::

## Queries and Mutations

A `query()` is read only, so small payloads travel in the url as a GET. A `mutation()` changes data, so it always travels in the body as a POST. A plain `route()` also travels in the body.

::code-split
<code-import path="packages/private-examples/src/client/client-query-mutation.ts" lang="ts [client.ts]" />
<code-import path="packages/private-examples/src/client/query-mutation.routes.ts" lang="ts [server.routes.ts]" />
::

::tip
Cancelling a query is always safe, the worst case is wasted work on the server. A cancelled mutation may already have changed data. See [Cancellation and Timeouts](/rpc/client/cancellation-timeouts).
::

## Sanitizing Params

For every route the server registered with `sanitizeParams`, the client applies the transforms its params types declare (trim, lowercase, strip separators) locally, before local validation and before sending, so both ends see the same value.

::code-split
<code-import path="packages/private-examples/src/client/client-sanitize-params.ts" lang="ts [client.ts]" />
<code-import path="packages/private-examples/src/client/sanitize.routes.ts" lang="ts [server.routes.ts]" />
::

:read-more{to="/rpc/server/validation#sanitizing-params" title="Sanitizing Params"}

## Full Example

A complete client and server, with every call shape used on these pages, lives in the repository: [packages/private-examples/src/client](https://github.com/MionKit/mion/tree/main/packages/private-examples/src/client){target="_blank"}.

:spacer

## Type Reference

### Result

`call()` returns this 5-tuple instead of throwing.
Declared responses are strongly typed: route in slots 0-1, middleware in slots 3-4.
The `undeclared` slot is an open `RpcError<string>`.

<code-import path="packages/rpc-client/src/types.ts" lang="ts" commentStart="// type-result-start" commentEnd="// type-result-end" />

### RemoteApi

Your API schema as the client uses it: a map of your public routes and middleware to `PublicRoute`, `PublicMiddleware` or `PublicHeadersFn`.

<code-import path="packages/rpc-router/src/types/publicMethods.ts" lang="ts" commentStart="// type-remote-api-start" commentEnd="// type-remote-api-end" />

### SubRequest

<code-import path="packages/rpc-client/src/types.ts" lang="ts" commentStart="// type-sub-request-start" commentEnd="// type-sub-request-end" />

### RouteSubRequest

<code-import path="packages/rpc-client/src/types.ts" lang="ts" commentStart="// type-route-sub-request-start" commentEnd="// type-route-sub-request-end" />

### ClientMiddleware

<code-import path="packages/rpc-client/src/types.ts" lang="ts" commentStart="// type-client-middleware-start" commentEnd="// type-client-middleware-end" />

### RequestHandler

<code-import path="packages/rpc-client/src/types.ts" lang="ts" commentStart="// type-request-handler-start" commentEnd="// type-request-handler-end" />

### MiddlewareContext

<code-import path="packages/rpc-client/src/types.ts" lang="ts" commentStart="// type-middleware-context-start" commentEnd="// type-middleware-context-end" />

<!-- code-import-timestamp 1788544325963 -->
