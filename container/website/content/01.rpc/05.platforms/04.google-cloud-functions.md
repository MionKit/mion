---
title: Google Cloud Functions
description: Running mion APIs on Google Cloud Functions for serverless deployments.
toc: false
---

mion can run on Google Cloud Functions for serverless deployments. The `@mionjs/platform-gcloud` package provides a handler compatible with Cloud Functions events.

## Installation

```bash
npm install @mionjs/platform-gcloud @mionjs/router
```

## Quick Start

::code-group
<code-import path="packages/examples/src/gcloud/gcloud-handler.ts" lang="ts [src/index.ts]" />
<code-import path="packages/examples/src/gcloud/gcloud-routes.ts" lang="ts [src/routes.ts]" />
::

## Deployment

Deploy your function using the gcloud CLI:

```bash
gcloud functions deploy api \
  --runtime nodejs18 \
  --trigger-http \
  --allow-unauthenticated
```

## Configuration

You can pass configuration options to `createGoogleCFHandler`, among them `maxBodySize`: the request limit a route takes when its own option is unset and its types cannot say (128 KB by default). Google caps a request at 10 MB (1st gen) or 32 MB (2nd gen), which no option can raise: the adapter carries the 1st gen number as `GOOGLE_CF_MAX_BODY_SIZE_CAP`, and `maxBodySizeCap` overrides it on 2nd gen. The platform hands the handler the whole body, so the route limit is checked before parsing rather than mid-flight. Router options like `basePath` are set once in `createMionRouter`, see [Creating the Router](/rpc/server/routes#creating-the-router) and [Security](/rpc/server/security#body-size-limits).

<code-import path="packages/examples/src/gcloud/gcloud-config.ts" lang="ts" />

::tip
For optimal cold start performance, keep your function dependencies minimal and consider using Cloud Run for more control over the runtime environment.
::

:spacer

## Type Reference

### GoogleCFOptions

<code-import path="packages/platform-gcloud/src/types.ts" lang="ts" commentStart="// type-google-cf-options-start" commentEnd="// type-google-cf-options-end" />

