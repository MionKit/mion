---
title: Routes 
description: Learn how to define and register routes in mion APIs.
toc: false
---

mion uses RPC-style routing: a route is a function you call remotely.

The first parameter is always the [Call Context](#call-context). The other parameters come from the client, decoded and validated before the route runs.

You define no URLs or paths. The API is a plain object where every entry is a route or middleware, which makes remote methods easy to reference from the client.

mion still gives each route a URL for plain HTTP requests, and the [client](/rpc/client/client-overview) handles it for you.

## Key Terms

::card-group{class="md:grid-cols-3"}
  :::card
  ---
  title: Routes
  icon: i-lucide-route
  to: /rpc/server/routes
  ---
  Functions you can call remotely, each with its own URL.
  :::

  :::card
  ---
  title: Middleware
  icon: i-lucide-link
  to: /rpc/server/middleware
  ---
  [Middleware]{.text-highlighted} functions that run before or after a route. They can send and receive data but cannot be called directly.
  :::

  :::card
  ---
  title: Execution Chain
  icon: i-lucide-ellipsis
  to: /rpc/server/middleware#execution-chain
  ---
  The ordered list of functions that run when a route is called: any number of middleware and one route.
  :::

  :::card
  ---
  title: Call Context
  icon: i-lucide-box
  to: /rpc/server/routes#call-context
  ---
  Passed to every route and middleware. It holds the request, the response and the shared data.
  :::

  :::card
  ---
  title: RunTypes
  icon: i-lucide-scan-text
  to: /runtypes/introduction/about-mion-runtypes
  ---
  mion uses @mionjs/run-types to validate and serialize your types automatically.
  :::
::

## Creating the Router

Create the router once per app with `createMionRouter` and pass it every router option (base path, parser, context data factory and the rest). Every helper it returns (`route`, `query`, `mutation`, `middleware`, `headersFn`, `rawMiddleware`) gets those options by type.

<code-import path="packages/examples/src/router/full-example.routes.ts" lang="ts" commentStart="// start-create-router" commentEnd="// end-create-router" />

::tip
Create the router and call `initRoutes` in your routes file, then import that file from the server entry. A second `createMionRouter` call throws. Destructuring the helpers is fine: `const {route, middleware} = createMionRouter({...})`.
::

## Defining a Route

Declare a route with `mion.route`: the handler is the first argument and the `RouteOptions` the second.

<code-import path="packages/examples/src/router/routes-definition.routes.ts" lang="ts" />

::tip
Never give routes or middleware a type. Use the [`satisfies`](https://www.typescriptlang.org/docs/handbook/release-notes/typescript-4-9.html#the-satisfies-operator){target="_blank"} operator instead, so the router stays fully typed.
::

## Query and Mutation Handlers

The `query` and `mutation` helpers declare what a route does, so the client can choose how to send it. A `query` sends small payloads in the URL as a GET, a `mutation` always in the body. A `mion.route()` declares no intent, sends its data in the body and has no cache semantics.

<code-import path="packages/examples/src/router/query-mutation-definition.routes.ts" lang="ts" />

::tip
Prefer `mion.query()` and `mion.mutation()`: your code reads better, and it leaves room for future optimizations like caching layers.
::

## Undeclared Properties

By default, a param property your types do not declare is dropped while the request is decoded, not at validation. The `clone` and `compact` strategies rebuild the params from your declared type.

To reject the request instead, set `parser: {params: 'mutateStrict'}`. See [Validation](/rpc/server/validation#undeclared-properties).

<code-import path="packages/examples/src/router/parser-mutate-strict.routes.ts" lang="ts" />

## Registering Routes

Pass the routes object to `mion.initRoutes`, once per app. It initializes the router with the options given to `createMionRouter`, registers the routes and returns the API type the client uses.

<code-import path="packages/examples/src/router/registering-multiple.routes.ts" lang="ts" />

## Naming Routes

Name routes with valid JS variable names, not quoted keys.

<code-import path="packages/examples/src/router/no-recommended-names.routes.ts" lang="ts" />

::note
Route lookup does not decode the URL, so a name with characters that need URL encoding is never found.
::

## Sanitizing Params

A route can apply the transforms its param types declare (trim, lowercase, strip separators) after decoding and before validation, on the server and in the client. Turn it on with `sanitizeParams`, globally in `createMionRouter` or per route. See [Validation](/rpc/server/validation#sanitizing-params).

<code-import path="packages/examples/src/router/sanitize-params-example.routes.ts" lang="ts" />

## Request Size

Every route resolves a request limit from its own `maxBodySize` option, else from its params types when every one of them has a maximum, else from the platform adapter's `maxBodySize` (128 KB by default). Responses are not limited. See [Security](/rpc/server/security#body-size-limits) for the whole rule and the platform ceilings.

<code-import path="packages/examples/src/router/security-max-body-size.ts" lang="ts" />

## Keeping the Raw Body

Once the body is parsed mion drops the raw text, so `ctx.request.rawBody` reads empty in your handlers. Set `releaseRawBody: false` in `createMionRouter` to keep it, for an access log that records exactly what arrived.

## Global Response Headers

Set `globalResponseHeaders` in `createMionRouter` to add headers to every response. mion also sends
`x-build-version`, the API version your server was built from. A `bundleApi` client
[checks its routes](/rpc/client/bundled-api#checking-the-version-at-runtime) when that version
differs from its own. `apiVersionCheck: false` turns the header off.

```ts
const mion = createMionRouter({globalResponseHeaders: {'x-team': 'mion'}});
```

::tip
The platform adapter's own `defaultResponseHeaders` wins when both set the same name.
::

## Awaiting Every Step

mion awaits every middleware and route in the chain. Set `alwaysAwait: false` to skip the await on
synchronous handlers: faster, but the slowest requests can get slower under load.

```ts
const mion = createMionRouter({alwaysAwait: false});
```

:spacer

## Call Context

The `CallContext` holds all the data of the current call. Most of it is marked read-only because you should not change it, but it is not truly immutable, so you still can.

::tip
Never store a reference to the context or its properties in a route or middleware: it can leak memory. Every handler receives the context anyway.
::

### Sharing Data Between Middleware and Routes

Use the `shared` object of the call context to share data between middleware and routes.

<code-import path="packages/examples/src/router/sharing-data.ts" lang="ts" />

### Context Data Factory

Pass a `contextDataFactory` to `createMionRouter` to build the `shared` object. It runs on every request, before any route or middleware, and its return value types `ctx.shared` in every handler.

<code-import path="packages/examples/src/router/using-context.routes.ts" lang="ts" />

::note
A shared field that starts out empty needs a return type on the factory, `(): {user: User | null} => ({user: null})`, otherwise `ctx.shared.user` is typed as `null` and handlers cannot assign to it.
::

<!-- code-import-timestamp 1788544325744 -->
