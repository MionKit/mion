---
title: Request & Response
description: Understanding mion's request and response objects.
---

Every route and middleFn gets a [MionRequest](#mionrequest) and a [MionResponse](#mionresponse) in its [call context](/rpc/server/routes#call-context), the first parameter.

mion does not use the Fetch API `Request` and `Response`. It uses its own objects to support platforms that do not use them, such as Node.js and AWS Lambda.

For example, [`@mionjs/platform-node`](/rpc/platforms/node-js) uses node's `IncomingMessage` and `ServerResponse` and [`@mionjs/platform-aws`](/rpc/platforms/aws-lambda) uses `APIGatewayEvent` and `APIGatewayProxyResult`, but routes and middleFns always get a MionRequest and MionResponse.

::note
mion passes the parameters to routes and middleFns for you, so you rarely need the request or response objects. Just declare the parameters and their types.
::

## How Data Is Sent and Received

The URL path is the route's id, used to find the route. The request body is a JSON object: each key is a route or middleFn id, each value an array of its parameters. The response body uses the same keys, each holding that method's return value.

This lets **one HTTP request call several routes and middleFns**. For example, an auth middleFn can send and receive its own data next to the route, in the same request.

::code-group

```md [Example]
| Request Path | Request Body                      | Response Body                          |
| ------------ | --------------------------------- | -------------------------------------- |
| `/sayHello`  | `{"sayHello": ["John"] }`         | `{"sayHello": "Hello John."}`          |
| `/greetings` | `{"greetings": ["Adan", "Eve"] }` | `{"greetings": "Hello Adan and Eve."}` |
```

<code-import path="packages/examples/src/router/req-resp.routes.ts" lang="ts [routes]" />

::

::tip
The [Client](/rpc/client/client-overview) formats requests and parses responses for you.
::

## Error Handling in Responses

Returned errors go under the route id and thrown errors in `@thrownErrors`, see [Error Response Format](/rpc/server/error-handling#error-response-format).

:spacer

## Type Reference

### MionRequest

mion's request object, independent of the platform's native request.

<code-import path="packages/router/src/types/context.ts" lang="ts" commentStart="// type-mion-request-start" commentEnd="// type-mion-request-end" />

### MionResponse

mion's response object, independent of the platform's native response.

<code-import path="packages/router/src/types/context.ts" lang="ts" commentStart="// type-mion-response-start" commentEnd="// type-mion-response-end" />

### MionHeaders

mion's headers object, similar to the [Fetch API Headers](https://developer.mozilla.org/en-US/docs/Web/API/Headers){target="_blank"}.

<code-import path="packages/router/src/types/context.ts" lang="ts" commentStart="// type-mion-headers-start" commentEnd="// type-mion-headers-end" />

### ResponseBody

The response body: each method's result and any thrown errors.

<code-import path="packages/router/src/types/context.ts" lang="ts" commentStart="// type-response-body-start" commentEnd="// type-response-body-end" />

<!-- code-import-timestamp 1788543523592 -->
