/* eslint-disable @typescript-eslint/no-namespace */
/* ########
 * 2025 mion
 * Author: Ma-jerez
 * License: MIT
 * The software is provided "as is", without warranty of any kind.
 * ######## */

// Import all format types with new naming convention
import type {FormatString} from './string/stringFormat.runtype';
import type {FormatDate} from './string/date.runtype';
import type {FormatDateTime} from './string/dateTime.runtype';
import type {FormatTime} from './string/time.runtype';
import type {
  FormatEmail,
  FormatEmailStrict,
  FormatEmailPattern,
  FormatEmailPunycode,
} from './string/email.runtype';
import type {FormatDomain, FormatDomainStrict} from './string/domain.runtype';
import type {
  FormatUrl,
  FormatUrlHttp,
  FormatUrlFile,
  FormatUrlSocialMedia,
} from './string/url.runtype';
import type {
  FormatIP,
  FormatIPV4,
  FormatIPV6,
  FormatIPWithPort,
  FormatIPV4WithPort,
  FormatIPV6WithPort,
} from './string/ip.runtype';
import type {FormatUUIDV4, FormatUUIDV7} from './string/uuid.runtype';
import type {
  FormatAlphaNumeric,
  FormatAlpha,
  FormatNumeric,
  FormatLowercase,
  FormatUppercase,
  FormatCapitalize,
} from './string/defaultStringFormats.runtype';

import type {FormatNumber} from './number/numberFormnat.runtype';
import type {
  FormatInteger,
  FormatFloat,
  FormatPositive,
  FormatNegative,
  FormatPositiveInteger,
  FormatNegativeInteger,
  FormatInt32,
  FormatUint32,
  FormatInt64,
} from './number/defaultNumberFormats';

import type {FormatBigInt} from './bigint/bigIntFormat.runtype';

// ############### String Formats Namespace ###############

export namespace SFormat {
  // Main StringFormat export
  export type StringFormat = FormatString;

  // Date/Time formats
  export type Date = FormatDate;
  export type DateTime = FormatDateTime;
  export type Time = FormatTime;

  // Network/Web formats
  export type Email = FormatEmail;
  export type EmailStrict = FormatEmailStrict;
  export type EmailPattern = FormatEmailPattern<RegExp, any>;
  export type EmailPunycode = FormatEmailPunycode;

  export type Domain = FormatDomain;
  export type DomainStrict = FormatDomainStrict;

  export type Url = FormatUrl;
  export type UrlHttp = FormatUrlHttp;
  export type UrlFile = FormatUrlFile;
  export type UrlSocialMedia = FormatUrlSocialMedia;

  export type IP = FormatIP;
  export type IPV4 = FormatIPV4;
  export type IPV6 = FormatIPV6;
  export type IPWithPort = FormatIPWithPort;
  export type IPV4WithPort = FormatIPV4WithPort;
  export type IPV6WithPort = FormatIPV6WithPort;

  // Identifier formats
  export type UUID = FormatUUIDV4; // default to V4
  export type UUID_V4 = FormatUUIDV4;
  export type UUID_V7 = FormatUUIDV7;

  // Default string formats
  export type AlphaNumeric = FormatAlphaNumeric;
  export type Alpha = FormatAlpha;
  export type Numeric = FormatNumeric;
  export type Lowercase = FormatLowercase;
  export type Uppercase = FormatUppercase;
  export type Capitalize = FormatCapitalize;
}

// ############### Number Formats Namespace ###############

export namespace NFormat {
  // Main NumberFormat export
  export type NumberFormat = FormatNumber;

  // Default number formats
  export type Integer = FormatInteger;
  export type Float = FormatFloat;
  export type Positive = FormatPositive;
  export type Negative = FormatNegative;
  export type PositiveInteger = FormatPositiveInteger;
  export type NegativeInteger = FormatNegativeInteger;
  export type Int32 = FormatInt32;
  export type Uint32 = FormatUint32;
  export type Int64 = FormatInt64;
}

// ############### BigInt Formats Namespace ###############

export namespace BIFormat {
  // Main BigIntFormat export
  export type BigIntFormat = FormatBigInt;
}
