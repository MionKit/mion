{
  "name": "@mionkit/gcloud",
  "version": "0.7.2",
  "description": "Google Cloud functions wrapper for mion router.",
  "keywords": [
    "typescript",
    "API",
    "RPC",
    "json",
    "schema",
    "generate",
    "server",
    "serverless",
    "framework",
    "node"
  ],
  "author": "ma jerez <jerez.m82@gmail.com>",
  "homepage": "https://mion.io/",
  "license": "MIT",
  "main": ".dist/index.js",
  "module": ".dist/index.js",
  "types": ".dist/index.d.ts",
  "exports": {
    ".": {
      "require": "./.dist/index.js",
      "default": "./.dist/index.js"
    }
  },
  "directories": {
    "lib": ".dist"
  },
  "files": [
    ".dist"
  ],
  "repository": {
    "type": "git",
    "url": "git+https://github.com/MionKit/mion.git"
  },
  "publishConfig": {
    "access": "public"
  },
  "scripts": {
    "test": "jest",
    "dev": "rimraf .dist && tsc --build tsconfig.json --watch",
    "dev:test": "jest --watch",
    "lint": "eslint src --ext .ts",
    "format": "prettier --write src/**/*.ts",
    "build": "tsc --project tsconfig.build.json --module NodeNext --outDir .dist",
    "auto-readme": "embedme **/*.md && prettier --write **/*.md",
    "clean": "rimraf .dist & rimraf .coverage"
  },
  "bugs": {
    "url": "https://github.com/MionKit/mion/issues"
  },
  "dependencies": {
    "@google-cloud/functions-framework": "^3.3.0",
    "@mionkit/core": "^0.7.2",
    "@mionkit/router": "^0.7.2"
  },
  "devDependencies": {
    "@types/express": "^4.17.20"
  }
}
