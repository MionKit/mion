/* ########
 * 2021 mion
 * Author: Ma-jerez
 * License: MIT
 * The software is provided "as is", without warranty of any kind.
 * ######## */

import {Type} from '@sinclair/typebox';
import {typeBox} from './reflection';

describe('Other typescript util types that are supported by Typebox', () => {
    it('never', () => {
        // todo remove later
    });
    // it('KeyOf', () => {
    //     type T = never;
    //     const boxType = typeBox<T>();
    //     expect(boxType).toEqual(Type.KeyOf());
    // });
    // it('Not', () => {
    //     type T = never;
    //     const boxType = typeBox<T>();
    //     expect(boxType).toEqual(Type.Not());
    // });
    // it('Extract', () => {
    //     type T = never;
    //     const boxType = typeBox<T>();
    //     expect(boxType).toEqual(Type.Never());
    // });
    // it('Exclude', () => {
    //     type T = never;
    //     const boxType = typeBox<T>();
    //     expect(boxType).toEqual(Type.Exclude());
    // });
    // it('Mapped', () => {
    //     type T = never;
    //     const boxType = typeBox<T>();
    //     expect(boxType).toEqual(Type.Mapped());
    // });
    // it('Record', () => {
    //     type T = Record<string, string>;
    //     const boxType = typeBox<T>();
    //     expect(boxType).toEqual(Type.Record(Type.String(), Type.String()));
    // });
    // it('Partial', () => {
    //     type T = never;
    //     const boxType = typeBox<T>();
    //     expect(boxType).toEqual(Type.Partial());
    // });
    // it('Required', () => {
    //     type T = never;
    //     const boxType = typeBox<T>();
    //     expect(boxType).toEqual(Type.Required());
    // });
    // it('Pick', () => {
    //     type T = never;
    //     const boxType = typeBox<T>();
    //     expect(boxType).toEqual(Type.Pick());
    // });
    // it('Omit', () => {
    //     type T = never;
    //     const boxType = typeBox<T>();
    //     expect(boxType).toEqual(Type.Omit());
    // });
    // it('Index', () => {
    //     type T = never;
    //     const boxType = typeBox<T>();
    //     expect(boxType).toEqual(Type.Index());
    // });
    // it('Uncapitalize', () => {
    //     type T = never;
    //     const boxType = typeBox<T>();
    //     expect(boxType).toEqual(Type.Uncapitalize());
    // });
    // it('Capitalize', () => {
    //     type T = never;
    //     const boxType = typeBox<T>();
    //     expect(boxType).toEqual(Type.Capitalize());
    // });
    // it('Uppercase', () => {
    //     type T = never;
    //     const boxType = typeBox<T>();
    //     expect(boxType).toEqual(Type.Uppercase());
    // });
    // it('Lowercase', () => {
    //     type T = never;
    //     const boxType = typeBox<T>();
    //     expect(boxType).toEqual(Type.Lowercase());
    // });
});
