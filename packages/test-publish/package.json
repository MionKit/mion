{
  "name": "@mionkit/test-publish",
  "version": "0.7.2",
  "private": true,
  "type": "module",
  "description": "Build verification tests - validates that all mion packages work correctly after being built",
  "keywords": [
    "mion",
    "build",
    "tests",
    "verification",
    "e2e"
  ],
  "author": "ma jerez",
  "homepage": "https://mion.io/",
  "license": "MIT",
  "scripts": {
    "test": "echo 'Run verify to test built artifacts'",
    "verify": "npm run test:build-verification && npm run test:pure-functions",
    "test:build-verification": "vitest run",
    "test:pure-functions": "cd packages/pure-functions-test && vitest run",
    "dev:test": "vitest watch",
    "lint": "npx eslint packages/modules/src",
    "format": "prettier --write packages/modules/src/**/*.ts",
    "build": "tsc --project tsconfig.json",
    "clean": "rimraf .dist",
    "fresh-start": "npm run clean && rimraf node_modules"
  },
  "dependencies": {
    "@mionkit/platform-aws": "^0.7.2",
    "@mionkit/platform-bun": "^0.7.2",
    "@mionkit/client": "^0.7.2",
    "@mionkit/core": "^0.7.2",
    "@mionkit/platform-gcloud": "^0.7.2",
    "@mionkit/platform-node": "^0.7.2",
    "@mionkit/router": "^0.7.2",
    "@mionkit/run-types": "^0.7.2",
    "@mionkit/test-server": "^0.7.2",
    "@mionkit/type-formats": "^0.7.2"
  },
  "devDependencies": {
    "dom-storage": "^2.1.0",
    "@mionkit/devtools": "^0.8.0-alpha.0"
  }
}
