/* ########
 * 2026 mion
 * Author: Ma-jerez
 * License: MIT
 * The software is provided "as is", without warranty of any kind.
 * ######## */

// Types both halves of mion's shared middlewares use; each server half is in `@mionjs/router/middlewares`, each client half in `@mionjs/client/middlewares`.

export type {RouteSyncError, RouteSyncErrorData, SyncRoutesHandler} from './src/types/middlewares.types.ts';
