{
  "name": "@apids/generate-fastify",
  "version": "0.0.0",
  "description": "package to generate fastify server from apis spec and typescript models",
  "keywords": [
    "fastify",
    "rest",
    "api",
    "open",
    "server"
  ],
  "author": "ma jerez <jerez.m82@gmail.com>",
  "homepage": "https://github.com/apids/apids/tree/main/packages/generate-fastify#readme",
  "license": "MIT",
  "main": ".dist/generate-fastify.js",
  "types": ".dist/generate-fastify.d.ts",
  "directories": {
    "lib": ".dist",
    "test": "__tests__"
  },
  "files": [
    ".dist"
  ],
  "repository": {
    "type": "git",
    "url": "git+https://github.com/apids/apids.git"
  },
  "scripts": {
    "test": "jest",
    "dev": "rimraf .dist && tsc --watch --build tsconfig.build.json",
    "dev:test": "jest --watch",
    "lint": "eslint src --ext .ts",
    "format": "prettier --write src/**/*.ts",
    "build": "rimraf .dist && jest && tsc --build tsconfig.build.json"
  },
  "bugs": {
    "url": "https://github.com/apids/apids/issues"
  },
  "devDependencies": {
    "@types/jest": "^27.0.2",
    "@types/node": "^16.11.6",
    "@typescript-eslint/eslint-plugin": "^5.2.0",
    "@typescript-eslint/parser": "^5.2.0",
    "eslint": "^8.1.0",
    "jest": "^27.3.1",
    "prettier": "^2.4.1",
    "rimraf": "^3.0.2",
    "ts-jest": "^27.0.7",
    "typescript": "^4.4.4"
  }
}
