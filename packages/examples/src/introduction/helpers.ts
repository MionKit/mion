/** Helper functions used by eslint-pure-functions.routes.ts examples */
export const myFn = (x: number) => x + 1;
export const myFactory = () => (x: number) => x * 2;
export const myMapper = (x: number) => x.toString();
