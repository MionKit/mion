{
  "//": "Source-resolution typecheck for the examples package: drops project references + composite",
  "//2": "so @mionjs/* resolve to SOURCE via the root customConditions:[source] export condition (paths",
  "//3": "cleared for single module identity), so `tsc --noEmit` checks examples without a package build.",
  "//4": "This IS a CI gate (see .github/workflows/pull-requests.yml); it must stay at 0 errors.",
  "//5": "twoslash-test holds deliberate-error demos and comparison-typia needs uninstalled external",
  "//6": "deps, so both stay excluded. bun-types is in `types` because resolving @mionjs/* to source",
  "//7": "pulls platform-bun source into the program, and the root tsconfig only lists `node`.",
  "extends": "../../tsconfig.json",
  "compilerOptions": {
    "noEmit": true,
    "composite": false,
    "declaration": false,
    "declarationMap": false,
    "incremental": false,
    "skipLibCheck": true,
    "module": "NodeNext",
    "moduleResolution": "NodeNext",
    "target": "ES2022",
    "lib": ["ES2023", "DOM"],
    "types": ["node", "bun-types"],
    "paths": {}
  },
  "include": ["src"],
  "exclude": ["node_modules", ".dist", "src/twoslash-test", "src/run-types/comparison-typia.ts"]
}
