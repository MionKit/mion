/** Default values used in the real starter app files. CLI replaces these with user values. */
export const STARTER_DEFAULTS = {
    PREFIX: 'api/mion',
    BASE_PATH: '/api/mion',
    API_WORKSPACE: '@mion-app/api',
    SERVER_ENTRY: 'vercel-serverless.ts',
} as const;
