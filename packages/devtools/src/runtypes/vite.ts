// `unplugin.vite(options)` returns a Vite plugin or a small array Vite flattens, so `plugins: [runtypes()]` works.
import {unplugin} from '../core/unplugin.ts';

export * from '../core/unplugin.ts';
export default unplugin.vite;
