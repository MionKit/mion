{
  "name": "@ts-runtypes/go-be-sidecar",
  "version": "0.11.0",
  "private": true,
  "description": "JS sidecar the Go resolver drives for regex jobs it cannot run itself. Bundled by vite and embedded into the ts-runtypes binary via go:embed; never published.",
  "type": "module",
  "scripts": {
    "build": "vite build && vite build --config vite.hook.config.ts",
    "test": "vitest run",
    "clean": "node ../../scripts/lib/rmrf.mjs dist .coverage"
  },
  "devDependencies": {
    "vite": "8.0.16"
  },
  "dependencies": {
    "randexp": "0.5.3"
  }
}
