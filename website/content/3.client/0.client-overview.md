---
title: Client Overview
description: Learn how to use the mion client for type-safe API calls.
toc: false
---

## Features

- ✅ Strongly typed APIs with autocompletion and static type checking.
- ✅ Fully typed list of remote methods with its parameters and return values.
- ✅ Result pattern with 4-tuple `[routeResult, routeError, hooksResults, hooksErrors]` - never throws, always returns.
- ✅ Automatic Validation and Serialization out of the box.
- ✅ Local Validation (no need to make a server request to validate parameters)
- ✅ Prefill request data to persist across multiple calls.


## Using the client

To use mion client we just need to initialize the client using the `RemoteApi` type returned from `registerRoutes`.
It is important to just import the type so we don't import any of the actual routes or backend code into the client.

<code-import ts="1768770126292" path="packages/examples/src/client/client-init.ts" lang="ts" />

::note
At the moment the client makes an initialization request to get routes and hooks metadata required for local validation and serialization.
Use AOT compilation to avoid this initialization request.
::

### Calling Routes

The `routes` and `hooks` objects returned from `initClient` contain all remote routes and hooks.
If a hook is not public (does not expect any parameters and can't return data) then it is not included within the `hooks` object.

Calling routes in the client generates a `RouteSubRequest`. We use the `call()` method to perform the remote call.
The `call()` method returns a 4-tuple `[routeResult, routeError, hooksResults, hooksErrors]` - it **never throws**.

<code-import ts="1768770126238" path="packages/examples/src/client/client-calling-routes.ts" lang="ts" />

### Using Hooks

For routes that require hooks, use `callWithHooks()` to pass hook data alongside the route call:

<code-import ts="1768770126382" path="packages/examples/src/client/client-using-hooks.ts" lang="ts" />

### Prefilling Hooks data

For cases like authorization hooks that are required for all requests, we can use the `prefill()` method of `HookSubRequest`. This will automatically prefill any future route calls that require that hook.

When using prefilled hooks, hook results and errors are available in **two ways**:
1. **TypedEvent handlers** - Strongly typed by errors
2. **tuple results** - Generic `RpcError` types

<code-import ts="1768770126318" path="packages/examples/src/client/client-prefill-hooks.ts" lang="ts" />

::note
**TypedEvent handlers** are called after **every** API request that includes the hook.
::

::tip
Use **TypedEvent handlers** for global error handling with prefilled hooks. They provide **strongly typed** error handling based on the error `type` string.
::

## Full example

::code-group
<code-import ts="1768770126257" path="packages/examples/src/client/client-full-example.ts" lang="ts [Client]" />
<code-import ts="0" path="packages/examples/src/router/server-full-example.ts" lang="ts [server.routes.ts]" />
::

:spacer

## Type Reference

### Result

The `Result` type is a 4-tuple used by `call()` and `callWithHooks()` to return route and hook results without throwing.

<code-import ts="1768770126574" path="packages/examples/src/client/types/client-types.ts" lang="ts" commentStart="// start-result" commentEnd="// end-result" />

### CallWithHooksResult

The result type returned by `callWithHooks()`. Returns a 4-tuple `[routeResult, routeError, hooksResults, hooksErrors]`.

<code-import ts="1768770126574" path="packages/examples/src/client/types/client-types.ts" lang="ts" commentStart="// start-call-with-hooks-result" commentEnd="// end-call-with-hooks-result" />

### RemoteApi

This type could be considered your API schema to be used by the client. This is a Mapping of your public hooks and routes to `PublicMethod`.

<code-import ts="1768770126574" path="packages/examples/src/client/types/client-types.ts" lang="ts" commentStart="// start-remote-api" commentEnd="// end-remote-api" />

### SubRequest

<code-import ts="1768770126574" path="packages/examples/src/client/types/client-types.ts" lang="ts" commentStart="// start-sub-request" commentEnd="// end-sub-request" />

### RouteSubRequest

<code-import ts="1768770126574" path="packages/examples/src/client/types/client-types.ts" lang="ts" commentStart="// start-route-sub-request" commentEnd="// end-route-sub-request" />

### HookSubRequest

<code-import ts="1768770126574" path="packages/examples/src/client/types/client-types.ts" lang="ts" commentStart="// start-hook-sub-request" commentEnd="// end-hook-sub-request" />
