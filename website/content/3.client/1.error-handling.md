---
title: Error Handling
description: Learn how to handle errors in the mion client using the Result pattern.
toc: false
---

The mion client uses a **Result pattern** for error handling. All API calls return a 4-tuple `[routeResult, routeError, hooksResults, hooksErrors]` - they **never throw**. This provides type-safe error handling with full TypeScript support.

## The Result Pattern

Every `call()` and `callWithHooks()` returns a 4-tuple:

<code-import ts="0" path="packages/client/examples/types/error-types.ts" lang="ts" commentStart="// start-result" commentEnd="// end-result" />

This means:
- Position 0: Route result (if successful)
- Position 1: Route error (if failed)
- Position 2: Hook results (keyed by hook name)
- Position 3: Hook errors (keyed by hook name)

### Basic Usage

<code-import ts="0" path="packages/client/examples/error-handling-basic.ts" lang="ts" />

## Handling Typed Errors

When routes define specific error types, TypeScript provides full autocompletion and type checking:

::code-group
<code-import ts="0" path="packages/client/examples/error-handling-typed.ts" lang="ts [Client]" />
<code-import ts="0" path="packages/router/examples/error-handling-typed-server.ts" lang="ts [server.routes.ts]" />
::

## Using callWithHooks

When you need to pass hook data with a route call (like authentication), use `callWithHooks()`. This method returns a 4-tuple that includes both route and hook results.

### CallWithHooksResult Structure

<code-import ts="0" path="packages/client/examples/types/error-types.ts" lang="ts" commentStart="// start-call-with-hooks-result" commentEnd="// end-call-with-hooks-result" />

### Basic callWithHooks Usage

::code-group
<code-import ts="0" path="packages/client/examples/error-handling-call-with-hooks.ts" lang="ts [Client]" />
<code-import ts="0" path="packages/router/examples/error-handling-hooks-server.ts" lang="ts [server.routes.ts]" />
::

## Prefilling Hooks

For hooks that should be included in all requests (like authentication), use `prefill()`:

<code-import ts="0" path="packages/client/examples/error-handling-prefill.ts" lang="ts" commentStart="// start-basic" commentEnd="// end-basic" />

### TypedEvent for Prefilled Hooks

When you prefill a hook, you can register persistent success/error handlers using `TypedEvent`:

<code-import ts="0" path="packages/client/examples/error-handling-typed-event.ts" lang="ts" />

## Prefill vs callWithHooks

| Aspect | `.prefill()` | `.callWithHooks()` |
|--------|--------------|-------------------|
| **Persistence** | Hook data stored for ALL future requests | Hook data sent with this request only |
| **Error Handlers** | Persistent via `.onError()` | Check `hookErrors` in 4-tuple |
| **Use Case** | Auth tokens, session data | Metadata related to the request |

## Complete Example

::code-group
<code-import ts="0" path="packages/client/examples/error-handling-complete.ts" lang="ts [Client]" />
<code-import ts="0" path="packages/router/examples/error-handling-complete-server.ts" lang="ts [server.routes.ts]" />
::

:spacer

## Type Reference

### Result

<code-import ts="0" path="packages/client/examples/types/error-types.ts" lang="ts" commentStart="// start-result" commentEnd="// end-result" />

### CallWithHooksResult

<code-import ts="0" path="packages/client/examples/types/error-types.ts" lang="ts" commentStart="// start-call-with-hooks-result" commentEnd="// end-call-with-hooks-result" />

### TypedEvent

<code-import ts="0" path="packages/client/examples/types/error-types.ts" lang="ts" commentStart="// start-typed-event" commentEnd="// end-typed-event" />

### RpcError

<code-import ts="0" path="packages/client/examples/types/error-types.ts" lang="ts" commentStart="// start-rpc-error" commentEnd="// end-rpc-error" />
